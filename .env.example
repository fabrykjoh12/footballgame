# Ball Knowledge environment configuration.
#
# Online multiplayer activates ONLY when one of the providers below is configured.
# With no keys, the app is fully playable offline vs the local CPU.
#
# --- Ably (real-time, option A) ---
VITE_ABLY_KEY=

# --- Supabase (real-time, option B) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
