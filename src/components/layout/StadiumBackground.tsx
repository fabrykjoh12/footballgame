/**
 * The stage is a flat warm charcoal set on `body` — the editorial look wants no
 * decorative backdrop. Kept as a component so the shell import stays stable.
 */
export function StadiumBackground() {
  return null;
}
